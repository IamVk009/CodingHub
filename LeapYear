// Find whether the given year is a leap year.

// Just a reminder: leap years are those years in which the year’s number is either divisible by 4, but not divisible by 100, or divisible by 400 
// (for example, the year 2000 is a leap year, but the year 2100 will not be a leap year).

import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        // put your code here
        Scanner scan = new Scanner(System.in);
        int year = scan.nextInt();
        System.out.println (year % 4 == 0 && (year % 100 != 0 || year % 400 == 0) ? "Leap" : "Regular" );
        
    }
}
